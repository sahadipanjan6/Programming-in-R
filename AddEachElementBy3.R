# Write a R program to add 3 to each element in a given vector. 
# Print the original and new vector.

myVector <- seq(from = 1, to = 20, by = 2)
print("Original Vector is: ")
print(myVector)
outputVector <- myVector + 3
print("Output Vector is: ")
print(outputVector)