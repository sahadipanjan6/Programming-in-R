# Write a R program to list the distinct values in a vector from a given vector.

myVector1 <- c(1, 1, 2, 5, 4, 2, 8, 9, 4, 10)
outputVector <- unique(myVector1)
print("Distinct values are: ")
print(outputVector)