# Write a R program to compute sum, mean and product of a given vector elements.

vec <- c(1, 2, 3, 4, 5)
print(paste("Sum:", sum(vec)))
print(paste("Mean:", mean(vec)))
print(paste("Product:", prod(vec)))